define([],
  function() {
    "use strict";
    return {
      x: function $qix(elem, binders, link) {
        return {
          x: arguments
        };
      },
      y: function $qix(elem, binders, link) {
        return {
          y: arguments
        };
      },
    };
  });